<!doctype html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width,initial-scale=1">
        {{content_for_header}}
    </head>
    <body>
        {{ content_for_layout }}
        {% liquid
            assign path = request.path
            assign file_handle = path | replace: "/", "__"
        %}
        <script>
            console.log("layout == password")
            console.log("request.path", {{ request.path | json }})
            console.log("file_handle", {{ file_handle | json }})
        </script>
    </body>
</html>
